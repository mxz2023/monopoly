# 前端开发时直连后端的端口（需与 npm run server 一致）。切勿设为 3000（与 Vite 冲突会导致 /api 挂死）。
VITE_DEV_WS_PORT=8080

# 生产构建后若静态页与 WS 不同域，可设完整地址，例如 wss://api.example.com/ws
# VITE_WS_URL=wss://your-host/ws

# 服务端 JWT 签名（生产环境务必设置长随机串）
# JWT_SECRET=your-long-random-secret
